import React from "react";
import { View, Text, Button } from "react-native";

const Supervisor = () => {
  return (
    <View>
      <Text>I'm a Supervisor</Text>
    </View>
  );
};

export default Supervisor;
